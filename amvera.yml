meta:
  environment: node
  toolchain:
    name: npm
    version: "18"

build:
  skip: true
  additionalCommands: true
  cp -r love_bot/* .

  artifacts:
    "*": /

run:
  scriptName: ""
  scriptArguments: null
  nodeArguments: null
  command: "npm run start"
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"

